-- ============================================
-- 다중 테이블 쿼리
-- ============================================
-- 01_schema.sql과 02_sample_data.sql을 먼저 실행한 후
-- 아래 TODO를 완성하세요.

-- ============================================
-- TODO 1: 특정 사용자의 모든 할일 (JOIN)
-- ============================================
-- users 테이블과 todos 테이블을 JOIN하여
-- 특정 사용자의 모든 할일을 조회하세요.
-- 출력 컬럼: username, title, priority, done
-- 힌트: SELECT u.username, t.title, t.priority, t.done
--        FROM todos t
--        JOIN users u ON t.user_id = u.id
--        WHERE u.username = '사용자이름';



-- ============================================
-- TODO 2: 사용자별 할일 수 통계
-- ============================================
-- 각 사용자별 전체 할일 수, 완료 수, 미완료 수를 조회하세요.
-- 출력 컬럼: username, total, done_count, undone_count
-- 힌트: GROUP BY와 집계 함수를 사용하세요.



-- ============================================
-- TODO 3: 카테고리별 할일 목록 (JOIN)
-- ============================================
-- categories 테이블과 todos 테이블을 JOIN하여
-- 카테고리별 할일 목록을 조회하세요.
-- 출력 컬럼: category_name, title, priority, done
-- 힌트: 3개 테이블을 JOIN할 수도 있습니다 (users + categories + todos).



-- ============================================
-- TODO 4: 완료율이 가장 높은 사용자
-- ============================================
-- 각 사용자의 할일 완료율을 계산하고,
-- 완료율이 가장 높은 사용자를 조회하세요.
-- 출력 컬럼: username, total, done_count, done_rate
-- 힌트: ORDER BY와 LIMIT를 사용하세요.



-- ============================================
-- TODO 5: 할일이 없는 카테고리 찾기 (LEFT JOIN)
-- ============================================
-- LEFT JOIN을 사용하여 할일이 하나도 없는 카테고리를 찾으세요.
-- 출력 컬럼: category_name, username
-- 힌트: LEFT JOIN 후 WHERE t.id IS NULL 조건 사용

