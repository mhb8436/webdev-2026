-- ============================================
-- 할일 통계 쿼리 (심화)
-- ============================================
-- 01_setup.sql을 먼저 실행한 후 이 파일의 TODO를 완성하세요.
-- 각 TODO의 주석을 참고하여 SQL 쿼리를 작성하세요.

-- ============================================
-- TODO 1: 카테고리별 할일 수
-- ============================================
-- GROUP BY를 사용하여 카테고리별 할일 개수를 조회하세요.
-- 출력 컬럼: category, count
-- 힌트: SELECT category, COUNT(*) AS count FROM todos GROUP BY ...;



-- ============================================
-- TODO 2: 우선순위별 완료율
-- ============================================
-- 각 우선순위(high, medium, low)에서 완료된 비율을 구하세요.
-- 출력 컬럼: priority, total(전체 수), done_count(완료 수), done_rate(완료율)
-- 힌트: SUM(done)은 완료된 할일의 수와 같습니다 (done은 0 또는 1)
-- 힌트: 완료율 = ROUND(SUM(done) * 100.0 / COUNT(*), 1)



-- ============================================
-- TODO 3: 할일이 3개 이상인 카테고리만 조회 (HAVING)
-- ============================================
-- GROUP BY 후 HAVING으로 그룹 조건을 걸어보세요.
-- 출력 컬럼: category, count
-- 힌트: HAVING COUNT(*) >= 3



-- ============================================
-- TODO 4: 가장 최근에 만든 할일 3개 (ORDER BY, LIMIT)
-- ============================================
-- created_at 기준 내림차순 정렬 후 3개만 조회하세요.
-- 힌트: ORDER BY created_at DESC LIMIT 3



-- ============================================
-- TODO 5: 완료되지 않은 할일 중 우선순위가 'high'인 것
-- ============================================
-- 서브쿼리 또는 WHERE 절의 AND 조합으로 조회하세요.
-- 방법 1: WHERE done = 0 AND priority = 'high'
-- 방법 2: 서브쿼리를 사용해보세요



-- ============================================
-- TODO 6: 통계 뷰 만들기 (CREATE VIEW)
-- ============================================
-- 카테고리별 전체, 완료, 미완료 수를 보여주는 뷰를 생성하세요.
-- 뷰 이름: todo_category_stats
-- 출력 컬럼: category, total, done_count, undone_count
-- 힌트: SUM(done)은 완료 수, SUM(CASE WHEN done = 0 THEN 1 ELSE 0 END)은 미완료 수



-- 뷰 조회
-- SELECT * FROM todo_category_stats;

-- ============================================
-- TODO 7: CASE 문으로 우선순위 한글 표시
-- ============================================
-- high → '높음', medium → '보통', low → '낮음'으로 변환하세요.
-- 출력 컬럼: title, priority, priority_kr
-- 힌트: CASE priority WHEN 'high' THEN '높음' ... END AS priority_kr

