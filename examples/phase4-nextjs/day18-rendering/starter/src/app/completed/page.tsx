// 완료 목록 페이지 - 서버 컴포넌트
// TODO: getCompletedTodos()로 완료된 할일만 가져와서 표시

export default async function CompletedPage() {
  // TODO: 서버에서 완료된 할일 가져오기
  // const todos = await getCompletedTodos();

  return (
    <main>
      <h1>완료된 할일</h1>
      {/* TODO: TodoApp에 완료된 할일만 전달 */}
    </main>
  );
}
