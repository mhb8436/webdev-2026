// 통계 페이지 - 서버 컴포넌트
// TODO: 서버에서 데이터를 가져와 TodoStats 서버 컴포넌트에 전달

export default async function StatsPage() {
  // TODO: 서버에서 할일 데이터 가져오기
  // const todos = await getTodos();

  return (
    <main>
      <h1>할일 통계</h1>
      {/* TODO: TodoStats 서버 컴포넌트에 데이터 전달 */}
    </main>
  );
}
