// ============================================
// Day 01 - 배열 심화
// ============================================
// 학습목표: 배열 생성, 접근, 수정, 주요 메서드

// TODO 1: 배열 생성과 접근
// fruits 배열: ["사과", "바나나", "오렌지", "포도", "딸기"]
// 첫번째, 마지막 요소 출력, 배열 길이 출력


// TODO 2: 배열 수정 메서드
// .push("수박") - 끝에 추가
// .pop() - 끝에서 제거
// .unshift("체리") - 앞에 추가
// .shift() - 앞에서 제거
// .splice(1, 1, "키위", "망고") - 인덱스 1에서 1개 삭제 후 추가


// TODO 3: 배열 탐색
// .indexOf("바나나"), .includes("포도")
// numbers = [10, 25, 30, 45, 50] 에서
// .find(n => n > 20), .findIndex(n => n > 20)


// TODO 4: 배열 반복
// for문, for...of문, .forEach()로 fruits 출력


// TODO 5: 배열 변환
// prices = [1000, 2000, 3000]
// .map(p => p * 1.1) - 세금 포함 가격
// scores = [85, 92, 45, 78, 95, 60, 33]
// .filter(s => s >= 60) - 60점 이상만
// .reduce((sum, s) => sum + s, 0) - 합계
// [30, 10, 50, 20].sort((a, b) => a - b) - 오름차순 정렬
